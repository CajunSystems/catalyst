<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.cajunsystems</groupId>
        <artifactId>catalyst-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>catalyst-api</artifactId>
    <name>Catalyst API</name>
    <description>Thin facade: Catalyst.embedded(...), builders, Serializers.</description>

    <dependencies>
        <dependency>
            <groupId>com.cajunsystems</groupId>
            <artifactId>catalyst-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.cajunsystems</groupId>
            <artifactId>catalyst-runtime</artifactId>
        </dependency>
        <dependency>
            <groupId>com.cajunsystems</groupId>
            <artifactId>catalyst-gumbo</artifactId>
        </dependency>
        <dependency>
            <groupId>com.cajunsystems</groupId>
            <artifactId>catalyst-tools</artifactId>
        </dependency>
        <dependency>
            <groupId>com.cajunsystems</groupId>
            <artifactId>catalyst-otel</artifactId>
        </dependency>
        <dependency>
            <groupId>com.cajunsystems</groupId>
            <artifactId>catalyst-timeline</artifactId>
        </dependency>
        <!--
          The auto-capture exit demo attaches the agent in-process. Optional for the same reason as the
          OTel SDK below: an app that never wants bytecode instrumentation should not inherit Byte Buddy
          from the facade. It stays on this module's own classpath so the demo builds and CI can gate it.
        -->
        <dependency>
            <groupId>com.cajunsystems</groupId>
            <artifactId>catalyst-agent</artifactId>
            <optional>true</optional>
        </dependency>
        <!--
          The OTel exit demo (Demo.java, main scope) builds a real SDK to collect the exported spans
          in-process. Marked optional so it stays off the transitive classpath of catalyst-api's
          consumers — the facade must not force the SDK on apps (they supply their own; catalyst-otel
          itself depends on the API only). It remains on this module's own compile/runtime/test
          classpath, so the demo builds and the CI gate can run it.
        -->
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-sdk</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-sdk-testing</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
